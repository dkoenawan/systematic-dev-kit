---
domain: <name>
last_updated: <YYYY-MM-DD>
source_path: <relative-path>
---

# <Domain Name>

## What Is <Domain Name>?

<2–4 sentences: what concept, object, or capability this domain represents in the product.
Frame it from the product's perspective, not the code's. E.g. "A WeeklyAssessment is a
structured evaluation that a coach completes for each athlete at the end of each training week.
It captures performance scores, qualitative notes, and recommended adjustments for the next cycle.">

## How It Works

<End-to-end narrative: the lifecycle of this domain's primary object. How is it created?
What states does it move through? What triggers state transitions? What happens when it
is consumed, completed, or archived? Write this as a story, not a list.>

## Core Objects / Entities

| Object | Description |
| ------ | ----------- |
| `<ClassName>` | <What it represents; its key properties> |

## Code Map — Which Code Touches This

- **Models / Schema**: `<path/to/file>` — <what data shape or type is defined here>
- **Business Logic / Services**: `<path/to/file>` — <what rules and workflows live here>
- **API / Interface**: `<path/to/file>` — <routes, controllers, or CLI entry points>
- **Persistence**: `<path/to/file>` — <repositories, migrations, seed files>
- **External callers**: `<path/to/file in OTHER domain>` — <why it calls into this domain>

## Internal Architecture

<Any state machines, event systems, factory patterns, or notable design decisions inside
this domain. Omit this section if there are no patterns worth calling out.>

## Dependencies

- **Internal**: <other domains this domain calls — and why>
- **External**: <npm/pip/gem packages or external APIs this domain depends on>

## Gotchas

- <Non-obvious constraint, edge case, or known issue. If none found, write "None identified at time of writing.">

## Changelog

- <YYYY-MM-DD>: <What changed, or "Initial documentation generated by doc-maintainer.">
