---
last_updated: <YYYY-MM-DD>
generated_by: /systematic-dev-kit:doc-maintainer
---

# <Project Name> — Solution Design

> Auto-generated by `/systematic-dev-kit:doc-maintainer`. Last updated: <YYYY-MM-DD>.
> This document describes the architecture of <Project Name> as understood from the codebase.
> It is a living document — update by running `/systematic-dev-kit:doc-maintainer maintain`.

---

## 1. Project Purpose

### What It Does

<2–4 sentences: the core capability of the system, from the end-user's perspective.>

### Who It's For

<1–2 sentences: the target user or operator. Infer from README, auth/role structures, and UI patterns.>

### Problem It Solves

<2–3 sentences: the before-state — what was harder or impossible without this system.>

### Non-Goals

<Bullet list of what the system explicitly does not do. Omit this section if not determinable from the codebase.>

---

## 2. Solution Architecture

### System Overview

<A 3–5 sentence narrative describing the whole system: what it is, how its major parts relate, and what runs where.>

### Architecture Diagram

```
<ASCII diagram showing the major components and their connections. Always include this,
even if simple. Example:

[Client Browser]
      |
      v
[React SPA] ──── REST API ──── [Express Server]
                                      |
                                [PostgreSQL DB]
                                      |
                            [Background Workers]
>
```

### Component Map

| Component | Technology | Role |
| --------- | ---------- | ---- |
| `<name>` | `<tech>` | <one-line role> |

### Data Flow

<Numbered list of the primary end-to-end flows. Example:
1. User logs in → JWT issued → token stored client-side → sent as Bearer on subsequent requests.
2. Task created → saved to DB → event emitted → notification worker sends email.>

### External Integrations

| Integration | Type | Purpose |
| ----------- | ---- | ------- |
| `<name>` | <REST API / SDK / Webhook / OAuth provider / etc.> | <why it's used> |

<If no external integrations exist, write: "None identified.">

### Infrastructure & Deployment

<2–3 sentences: how the system is deployed (Docker, cloud provider, CI/CD, environment split).
Omit this section if not determinable from the codebase.>

---

## 3. Domain Architecture

### Business Domains

<These domains implement core product value — user-visible features with business rules.>

| Domain | Path | Purpose | Docs |
| ------ | ---- | ------- | ---- |
| [<name>](./<name>/overview.md) | `<src/dir>` | <one-line purpose> | [overview](./<name>/overview.md) |

### Auxiliary Domains

<These domains provide cross-cutting infrastructure that supports the business domains.>

| Domain | Path | Purpose | Docs |
| ------ | ---- | ------- | ---- |
| [<name>](./<name>/overview.md) | `<src/dir>` | <one-line purpose> | [overview](./<name>/overview.md) |

---

## 4. Key Architectural Decisions

- **<Decision>**: <What was decided>. <Why, in one sentence.>
- **<Decision>**: <What was decided>. <Why, in one sentence.>

<Include 3–7 notable decisions that shaped the design. If fewer are evident, that's fine.>

---

## 5. Changelog

- <YYYY-MM-DD>: <brief description, or "Initial solution design generated by doc-maintainer.">
