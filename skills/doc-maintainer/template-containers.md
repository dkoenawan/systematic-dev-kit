---
last_updated: <YYYY-MM-DD>
generated_by: /systematic-dev-kit:doc-maintainer
---

# <Project Name> — Container Architecture (L2)

> → [System overview](solution-design.md) | → Domain details: links in Domain Map below.

---

## Container Diagram

```
<ASCII diagram: deployable units as boxes, connections as arrows.
Label each arrow with the protocol (HTTP, TCP, AMQP, etc.).
Keep it to what actually runs — no future plans.

Example:
[Browser]
    |
    | HTTPS
    v
[React SPA]  ←── static assets
    |
    | REST / Bearer JWT
    v
[API Server]
    |         |
    |         └── [Background Worker]
    v                    |
[PostgreSQL]         [External API]
>
```

---

## Services / Containers

| Name | Type | Technology | Purpose |
| ---- | ---- | ---------- | ------- |
| `<name>` | web / worker / db / cache / queue | `<tech + version>` | <one-line role> |

---

## Primary Data Flows

<Numbered narrative of the 2–4 main end-to-end flows across containers. Each flow should
be one or two sentences tracing the request/event from origin to outcome.

Example:
1. **Authentication**: Browser → POST /auth/login → API validates credentials → issues JWT → returned to browser.
2. **Background job**: API inserts row into jobs table → Worker polls table → processes job → marks complete.>

---

## Deployment Model

<1–2 sentences: how this is deployed (Docker Compose, k8s, serverless, single VPS, etc.).
Write "Not determined" if not visible in the codebase.>

---

## Changelog

- <YYYY-MM-DD>: L2 container architecture generated by doc-maintainer.
