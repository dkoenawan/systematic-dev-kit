# API Specs

> Reference-oriented API specifications, sourced from `plan`'s Backend Layer
> spec section during session fold-back. Populated as features generate API
> surfaces — none yet for this plugin repo (it has no server component).

See [ADR-001](../../registry/decisions/001-diataxis-docs-restructure.md) for how
this folder fits into the docs/ structure.
