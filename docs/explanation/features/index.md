# Feature Index

> Each file here traces one named feature end-to-end: FR/NFR → Pages → API
> specs → Technical architecture. Generated on-demand (by `doc-maintainer` or
> `plan` post-approval), not scheduled. See
> [ADR-001](../../registry/decisions/001-diataxis-docs-restructure.md).

## Features

(none yet — add entries as feature indexes are generated)
