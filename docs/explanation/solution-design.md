---
last_updated: 2026-05-01
generated_by: /compass:doc-maintainer
---

# Systematic Dev Kit — Solution Design (L1)

> Auto-generated by `/compass:doc-maintainer`. Last updated: 2026-05-01.
> **Reading guide**: This is the L1 entry point. Read this to orient. Drill into linked files only as needed.
> → Infrastructure: [containers.md](containers.md) | → Domain details: links in the Domain Map below.

---

## 1. System Context

### What It Does

Systematic Dev Kit is a Claude Code plugin that provides opinionated, reusable agent-driven skills for building production-ready full-stack applications. It encodes best practices as executable `/compass:<skill-name>` slash commands covering project scaffolding, feature planning, codebase exploration, brand identity design, and automated documentation maintenance. Each skill is an agent-driven workflow that operates directly on the developer's local filesystem and git history — no server, container, or cloud infrastructure required.

### Who It's For

Full-stack developers using Claude Code (Anthropic's CLI/IDE plugin) who want systematic, opinionated workflows rather than ad-hoc generation. Developers who value repeatability and token efficiency over flexibility.

### External Systems

| System | Purpose |
| ------ | ------- |
| `dev-kit-scaffolding` GitHub repo | Source template cloned by the `init` skill to bootstrap new projects |
| Google Fonts | Validated by `brand-designer` to confirm chosen font names are real and currently available |

### System Context Diagram

```
  [Developer / Claude Code CLI]
             |
             | invokes skill via /compass:<name>
             v
  ┌─────────────────────────────────────┐
  │         Systematic Dev Kit          │
  │   (Claude Code plugin — no server)  │
  └─────────────────────────────────────┘
        |                    |
        | git clone          | font validation (brand-designer only)
        v                    v
 [dev-kit-scaffolding]  [Google Fonts]
   GitHub template repo
```

---

## 2. Domain Map

> Each maintain run generates one missing domain overview. Status: 📄 = generated | ⬜ = not yet generated.

### Business Domains

| Domain | Path | Purpose | Status |
| ------ | ---- | ------- | ------ |
| [brand-designer](./brand-designer/overview.md) | `skills/brand-designer` | Six-phase emotional-discovery brand identity system producing guidelines, CSS tokens, and a live showcase | 📄 |
| [doc-maintainer](./doc-maintainer/overview.md) | `skills/doc-maintainer` | C4-layered documentation lifecycle: seeds, incrementally maintains, and refreshes `docs/` on a dedicated git branch | 📄 |
| [explore](./explore/overview.md) | `skills/explore` | Token-efficient tiered codebase investigation; returns structured architecture reports used by `plan` and `doc-maintainer` | 📄 |
| [init](./init/overview.md) | `skills/init` | Template-clone project scaffolding with opt-out component selection | 📄 |
| [plan](./plan/overview.md) | `skills/plan` | Adaptive five-phase feature specification generator producing layer-by-layer `FeatureSpec` documents | 📄 |
| [reviews](./reviews/overview.md) | `reviews` | Skill test-run records and failure-pattern library for improving skill definitions | 📄 |

### Auxiliary Domains

| Domain | Path | Purpose | Status |
| ------ | ---- | ------- | ------ |
| [bootstrap-new-project](./bootstrap-new-project/overview.md) | `skills/bootstrap-new-project` | Deprecated direct-generation scaffolding; superseded by `init` | 📄 |
| `backend` | `skills/backend` | Reserved stub — future backend-specific skill (currently empty) | — |
| `database` | `skills/database` | Reserved stub — future database-specific skill (currently empty) | — |
| `frontend` | `skills/frontend` | Reserved stub — future frontend-specific skill (currently empty) | — |
| `infrastructure` | `skills/infrastructure` | Reserved stub — future infrastructure skill (currently empty) | — |

→ Infrastructure detail: [containers.md](containers.md) ⬜

---

## 3. Key Architectural Decisions

- **Skill-as-Markdown**: All skill logic lives in `SKILL.md` files with YAML frontmatter — human-readable, diff-friendly, and model-agnostic. No compilation step; any Claude model can execute them.

- **Opt-out scaffolding over opt-in**: The `init` skill presents the full stack by default and asks what to *remove*, reducing cognitive load and keeping the full-stack happy path near-zero friction.

- **Template-clone over direct generation**: `init` clones a pre-built GitHub template rather than generating files from scratch — faster, lower-token, and keeps generated projects in sync with upstream template improvements.

- **Tiered codebase investigation**: The `explore` skill reads documentation before code (Tier 1), then structure files (Tier 2), then up to 5 targeted source files (Tier 3), stopping as soon as the focus is answered. This bounds token cost by task scope.

- **Dedicated docs branch**: `doc-maintainer` always operates on `chore/claude-maintain`, never on `main` or feature branches, keeping auto-generated commits separate from source history.

- **Approval gates before file generation**: Both `plan` and `brand-designer` require explicit user sign-off on synthesised briefs/specs before writing any files, preventing misinterpreted output from reaching disk.

- **Per-domain staleness tracking via frontmatter**: Each `docs/<domain>/overview.md` carries a `last_updated` YAML date; the `find-stale-domains.sh` script reads this to detect which domains need refresh, enabling targeted incremental updates.

---

## 4. Changelog

- 2026-04-24: Initial solution design generated by doc-maintainer.
- 2026-05-01: Full refresh via doc-maintainer.
